{
  "name": "json-mock-server",
  "version": "1.0.0",
  "description": "simple mock server using json-server",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:watch": "json-server --watch db.json --port 8080"
  },
  "author": "satoshicano <satoshicano@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
